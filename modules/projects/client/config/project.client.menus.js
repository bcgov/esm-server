'use strict';

angular.module('project').run(['Menus',
	function (Menus) {
		// Menus.addMenuItem('projectMenu', {
		// 	title: 'Activities',
		// 	state: 'activities',
		// 	roles: ['user', 'admin']
		// });
	}
]);
